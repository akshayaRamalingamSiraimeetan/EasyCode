/**
 * reviewPrompt.js — placeholder for future implementation
 *
 * Will build a prompt for: POST /review
 * Purpose: Review user's code for correctness, style, and efficiency.
 */

const buildReviewPrompt = ({ problem, language, userCode }) => {
  // TODO: implement
  throw new Error("reviewPrompt is not yet implemented.");
};

module.exports = buildReviewPrompt;
