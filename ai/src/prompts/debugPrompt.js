/**
 * debugPrompt.js — placeholder for future implementation
 *
 * Will build a prompt for: POST /debug
 * Purpose: Help user identify why their code produces wrong output.
 */

const buildDebugPrompt = ({ problem, language, userCode, failedTestCase }) => {
  // TODO: implement
  throw new Error("debugPrompt is not yet implemented.");
};

module.exports = buildDebugPrompt;
